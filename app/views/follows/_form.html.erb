<%= form_with(model: follow) do |form| %>
  <% if follow.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(follow.errors.count, "error") %> prohibited this follow from being saved:</h2>

      <ul>
        <% follow.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id_id, style: "display: block" %>
    <%= form.text_field :user_id_id %>
  </div>

  <div>
    <%= form.label :followed_id_id, style: "display: block" %>
    <%= form.text_field :followed_id_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
